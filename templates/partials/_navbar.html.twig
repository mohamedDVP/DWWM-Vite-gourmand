<nav class="navbar navbar-expand-lg site-header">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('img/logoV&G.png') }}" alt="Logo Vite & Gourmand">
        </a>

        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNavbar"
            aria-controls="mainNavbar"
            aria-expanded="false"
            aria-label="Ouvrir le menu"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">

            <ul class="navbar-nav mx-auto">

                <li class="nav-item">
                    <a href="{{ path('app_home') }}" class="nav-link {{ app.current_route == 'app_home' ? 'active' : '' }}">Accueil</a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_menus') }}" class="nav-link {{ app.current_route == 'app_menus' ? 'active' : '' }}">Menus</a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_contact') }}" class="nav-link {{ app.current_route == 'app_contact' ? 'active' : '' }}">Contact</a>
                </li>

            </ul>

            <a class="btn btn-primary bold" href="{{ path('app_login') }}">
                Connexion
            </a>

        </div>

    </div>
</nav>